# Count the user entered word in given string

str = input("Enter the string : ").lower()

# can also ue this
# str=str.lower()

word = input("Enter the word to count : ").lower()

# can also use this
# word=word.lower()

print(str.count(word))